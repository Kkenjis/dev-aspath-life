# ★ フェーズ9：本番切替（方式B）実行手順

作成日：2026年8月17日／実施予定：9月／対象：**`https://aspath-life.com`（本番）**
所要：**約3時間**（うちサイト停止は 約40分）

---

## 0. 方式Bとは

**DBをまるごと移行しません。** 本番に対して、テーマの適用とページ作成をやり直します。

> ✅ **このテーマは固定ページの中身をPHPが持っている**設計です。
> 本番にテーマを入れてスラッグを合わせるだけで、**9ページ分の見た目がそのまま再現**されます。
> だから移すべき実データは、投稿3本の本文・画像18点・キャンペーン投稿・フォームのページだけです。

| | 方式A（DB移行） | **方式B（採用）** |
|---|---|---|
| `/development/` の置換 | 52箇所＋設定を一括置換。漏れると画像が全部消える | **そもそも発生しない** |
| 本番の既存データ | 全部上書き | **残る**（触った部分だけ変わる） |
| 失敗時 | バックアップから全体復元 | **その項目だけ直す** |
| 移行プラグイン | 必要（WP STAGINGは無料版でPush不可） | **不要** |

---

## 1. 本番の現状（2026-08-17 実測）

公開サイトマップで確認した、本番にいま存在するものです。

### 固定ページ（7本）

| スラッグ | 判定 |
|---|---|
| `/`（HOME） | ✅ そのまま使う |
| `about` | ✅ そのまま使う（タイトルを日本語化） |
| `services` | ✅ そのまま使う（タイトルを日本語化） |
| `contact` | ✅ そのまま使う（タイトルを日本語化） |
| `blog` | ⚠ **スラッグを `column` に変更**（新規作成しない） |
| `for-parkinsons-disease` | 🗑 301設定後に削除 |
| `パーキンソン病の方へ` | 🗑 301設定後に削除 |
| `for-stroke` | 🗑 301設定後に削除 |

**本番に無い＝新規作成が必要（5本）**

`access` ／ `faq` ／ `tokushoho` ／ `sitemap` ／ `privacy`（下書きなら公開）

> ⚠ **山口様は現在も記事を書き続けておられます**（8/18に1本追加を確認）。
> 方式Bでは本番のデータベースを触らないため、**追加された記事は自動的に残ります**。
> 移行作業は不要です。**当日は本数の確認だけ**してください。

### 投稿（2026-08-17時点で6本 → 8/18に1本追加され7本）

| スラッグ | 割り当てるカテゴリー | 本文 |
|---|---|---|
| `aspathkouen` | **お知らせ** | そのまま |
| `shinsenaspath`（パーキンソン病の振るえとは？） | コラム | そのまま（※確認事項3） |
| `taimentraining` | コラム | **新デザインに差し替え** |
| `onlineaspath` | コラム | **新デザインに差し替え** |
| `パーキンソン病とアスパスの歩み方` | コラム | **新デザインに差し替え** |
| `ASPATH（アスパス）のサイトが公開されました！` | **お知らせ** | そのまま |

**本番に無い＝新規作成（1本）**：営業開始キャンペーン（`campaign`）

### カテゴリー

**`uncategorized` しかありません。** `info` と `column` を作る必要があります。

> ⚠ **本番の最終更新は 2026-08-14** でした。9月の実施前に、
> **記事が増えていないか必ず再確認**してください（増えていれば作業対象に足す）。

---

## 2. 事前準備（サイトを止めずにできる・前日までに）

### 2-1. 画像18点をアップロードする月を決める

コラム本文の画像URLには**アップロードした年月**が入ります。

- [ ] `_wp移行素材/★コラム本文_貼り付け用_本番/` の3本は **2026年9月** にアップする前提で作成済み
- [ ] **9月以外にアップした場合**は、貼り付け用HTMLを作り直す

```
python tools_columns_to_blocks.py --honban --tsuki 10
```

（10月にアップした場合の例。`_wp移行素材/★コラム本文_貼り付け用_本番/` が上書きされます）

> 判定方法：本番のメディアライブラリで `column-` の画像を1つ開き、
> ファイルのURLが `/2026/09/` か `/2026/10/` かを見るだけです。

### 2-2. 手元に揃えておくもの

- [ ] `テーマを作る.ps1` を実行して **最新の `aspath-theme.zip`**
- [ ] `_wp移行素材/★アップロード用_投稿画像18点/`（18点）
- [ ] `_wp移行素材/★コラム本文_貼り付け用_本番/`（3本）
- [ ] `_wp移行素材/★お知らせ_営業開始キャンペーン_インポート用.xml`
- [ ] `_wp移行素材/★メディア登録_alt一覧_18点.md`
- [ ] ステージングの初回体験フォームのスラッグ（`taiken-2026as9y`）

### 2-3. 本番のバックアップ ★絶対に省略しない

- [ ] 本番の管理画面でバックアップを取得（WP STAGING／サーバーのバックアップ機能）
- [ ] **バックアップから復元できることを確認**（ファイルが実際にダウンロードできているか）

> これが唯一の保険です。以降の作業はすべて「戻せる」前提で進みます。

---

## 3. サイトを止めずにできる作業（約60分）

**この章はすべて、訪問者から見て何も変わりません。** 落ち着いて進めてください。

### 3-1. カテゴリーを作る

投稿 → カテゴリー

| 名前 | スラッグ |
|---|---|
| お知らせ | **`info`** |
| コラム | **`column`** |

- [ ] 2つ作成
- [ ] 設定 → 投稿設定 → 既定の投稿カテゴリーを「**コラム**」に

> ⚠ **お知らせのスラッグは必ず `info`。** TOPのお知らせ枠がこのスラッグで記事を拾います。

### 3-2. 投稿にカテゴリーを割り当てる

投稿一覧で**一括編集**を使うと速いです。§1の表のとおりに割り当て。

- [ ] 6本すべてに割り当て
- [ ] 「未分類」が0件になる

### 3-3. 画像18点をアップロード

メディア → 新規追加 → `★アップロード用_投稿画像18点/` をまとめてドラッグ&ドロップ

- [ ] 18点をアップロード
- [ ] 代替テキスト（alt）を設定（`★メディア登録_alt一覧_18点.md` から転記）
- [ ] メディアライブラリで `column-` と検索して **18点**出る
- [ ] **画像1点のURLを確認し、年月をメモ**（§2-1の判定に使う）

### 3-4. 固定ページを5本作る

固定ページ → 新規追加。**タイトルとスラッグだけ。本文は空。**

| タイトル | スラッグ |
|---|---|
| 交通アクセス | `access` |
| よくあるご質問 | `faq` |
| 特定商取引法に基づく表記 | `tokushoho` |
| サイトマップ | `sitemap` |
| プライバシーポリシー | `privacy`（既にあれば公開＋スラッグ確認） |

- [ ] 5本を**公開**

> このページは旧テーマでは白紙に見えます。**メニューから辿れないので問題ありません。**
> テーマを切り替えた瞬間に、正しい中身が表示されます。
>
> ⚠ スラッグは**半角英字で表のとおり正確に**。1文字違うとテンプレートが当たらず白紙になります。

### 3-5. 「blog」を「コラム」に変える

- [ ] 固定ページ `blog` を開き、**タイトルを「コラム」・スラッグを `column`** に変更

> ⚠ **新規作成しないでください。** 新規だと投稿ページの指定が外れます。
> 旧 `/blog/` は §5-3 で301転送します。

### 3-6. 固定ページのタイトルを日本語にする

**スラッグは絶対に変えない。** クイック編集でタイトルだけ。

| スラッグ | 変更後のタイトル |
|---|---|
| `about` | ASPATHについて |
| `services` | プランと料金 |
| `contact` | お問い合わせ |

- [ ] 3本を変更（`HOME` は触らない）

### 3-7. キャンペーン投稿を作る

`★お知らせ_営業開始キャンペーン_登録手順.md` のとおり、XMLをインポート。

- [ ] ツール → インポート → WordPress → `★お知らせ_営業開始キャンペーン_インポート用.xml`
- [ ] カテゴリーが「お知らせ」になっている
- [ ] **「先頭に固定表示」にチェック**（これが「重要」バッジの条件）

### 3-8. 初回体験フォーム → ⏭ **この章は不要です**

**フォームは先行設置プラグインで既に本番に置いてあります。**
→ `★初回体験フォーム_本番先行設置_手順.md`

- [ ] プラグイン「**ASPATH 初回体験フォーム**」が**有効**であることだけ確認

> ⚠ **固定ページは作らないでください。** プラグインが `/taiken-2026as9y/` を持っています。
> 同じスラッグの固定ページを作ると衝突します。
>
> ⚠ テーマ側にもフォーム機能がありますが、**プラグインが有効なら自動的に何もしません**
> （二重に動かないようガードを入れてあります）。プラグインは有効のままにしてください。

---

## 4. サイトを止めて切り替える（約40分）★ここが本番

### 4-1. メンテナンスモードをON

- [ ] LightStart → メンテナンスモードを **ON**
- [ ] シークレットウィンドウで、訪問者にメンテナンス画面が出ることを確認

> **ここから40分間、サイトは非公開になります。** 平日の夜など、訪問の少ない時間帯を選んでください。

### 4-2. テーマを適用

- [ ] 外観 → テーマ → 新規追加 → テーマのアップロード → `aspath-theme.zip`
- [ ] インストール → **有効化**
- [ ] キャッシュを削除（Super Page Cache／Cloudflare）

### 4-3. 表示設定

設定 → 表示設定

- [ ] ホームページの表示：**固定ページ**
- [ ] ホームページ：**HOME**
- [ ] 投稿ページ：**コラム**
- [ ] 「検索エンジンでの表示を許可しない」の**チェックを外す**（本番は検索に載せる）

> ⚠⚠ **投稿ページに指定してよいのは「コラム」だけ、ホームページは「HOME」だけ。**
> 他を指定すると、そのページを開いてもコラム一覧が表示されます（実際に起きました）。

### 4-4. コラム3本の本文を差し替え

`★コラム本文_貼り付け用_本番/` の3ファイルを使います。記事ごとに5ステップ。

1. 投稿一覧から対象記事を開く
2. 右上の **⋮** → **コードエディター**
3. 中身を全選択して削除（`Ctrl+A` → `Delete`）
4. ファイルの中身を全部コピーして貼り付け
5. **⋮ → ビジュアルエディター** に戻して確認 → **更新**

- [ ] アスパススタジオでの対面トレーニング ← `taimentraining.html`
- [ ] アスパスのオンライントレーニングがおすすめ！ ← `onlineaspath.html`
- [ ] パーキンソン病とアスパスの歩み方 ← `column-parkinson.html`

### 4-5. アイキャッチを設定

| 記事 | アイキャッチ |
|---|---|
| アスパススタジオでの対面トレーニング | `column-taimentraining-thumbnail.webp` |
| アスパスのオンライントレーニングがおすすめ！ | `column-onlineaspath-thumbnail.webp` |
| パーキンソン病とアスパスの歩み方 | `column-parkinson-thumbnail.webp` |

- [ ] 3本すべて設定

### 4-6. フォームをキャッシュから除外

- [ ] Super Page Cache の「キャッシュしないページ」に `/taiken-2026as9y/` を追加
- [ ] キャッシュをパージ

> これを飛ばすと、送信時に「送信が無効になりました」が出ます。

### 4-7. 表示確認（メンテナンス中に）

管理画面にログインした状態なら、メンテナンス中でもサイトが見えます。

- [ ] `/` `/about/` `/services/` `/access/` `/contact/` `/faq/` `/privacy/` `/tokushoho/` `/sitemap/` が**それぞれのページ**として表示される
- [ ] `/column/` にコラム4本が並ぶ
- [ ] `/category/info/` にお知らせ3本、**「重要」が一番上**
- [ ] TOPのお知らせ・コラムが**各2件**
- [ ] コラム3本の**画像がすべて表示される**（1枚でも欠けたら§2-1の年月違い）
- [ ] `/taiken-2026as9y/` にフォームが表示される
- [ ] 存在しないURL（`/aaa/`）で「ページが見つかりません」が出る

### 4-8. フォームの送信テスト ★必須

- [ ] 実際に送信 → 緑の完了表示
- [ ] 山口様に通知メールが届く
- [ ] 入力したアドレスに控えメールが届く（**48時間以内**の記載あり）
- [ ] 管理画面「初回体験の申込」に残っている
- [ ] テスト分を削除

### 4-9. メンテナンスモードをOFF

- [ ] LightStart → メンテナンスモードを **OFF**
- [ ] シークレットウィンドウで、通常のサイトが見えることを確認

**ここで公開完了です。**

---

## 5. 公開後の作業（止めずにできる・約40分）

### 5-1. 公式LINEのURL → ⏭ **差し替え不要です**

先行設置により、**LINEには最初から本番URLが載っています**。
→ `★初回体験フォーム_本番先行設置_手順.md` §6

- [ ] 念のため、切替後に `https://aspath-life.com/taiken-2026as9y/` が開けることを確認
- [ ] LINEから実際にタップして、フォームが表示されることを確認

> ✅ **URLの差し替え作業は発生しません。** フェーズ9で最も事故が起きやすい箇所を、
> 先行設置によって構造的に無くしています。

### 5-2. 301リダイレクトを設定

`301リダイレクト対応表.md` のとおり。Redirection プラグイン等を使用（本番に未インストールなら追加）。

| 旧URL | 転送先 |
|---|---|
| `/for-parkinsons-disease/` | `/パーキンソン病とアスパスの歩み方/` |
| `/パーキンソン病の方へ/` | `/パーキンソン病とアスパスの歩み方/` |
| `/for-stroke/` | `/about/` |
| `/blog/` | `/column/` |

- [ ] 4本を設定
- [ ] 実際に旧URLを開いて転送されることを確認

### 5-3. 旧ページを削除

**301の動作確認が済んでから**削除します。

- [ ] `for parkinson's disease`（重複していれば両方）
- [ ] `for stroke`
- [ ] `パーキンソン病の方へ`
- [ ] 「サンプルページ」があれば削除

> `Customer Cabinet` は**削除しない**（予約システムが使用）。

### 5-4. カテゴリーの整理

- [ ] `uncategorized`（未分類）を削除（既定を「コラム」にしてから）

### 5-5. SEOの最終確認

- [ ] 設定 → 表示設定 → noindexの**チェックが外れている**
- [ ] `/taiken-2026as9y/` のソースに `noindex,nofollow,noarchive` が**ある**（テーマが自動出力）
- [ ] `https://aspath-life.com/wp-sitemap.xml` にフォームのページが**載っていない**
- [ ] Search Console にサイトマップを再送信
- [ ] www有無の正規化が旧サイトと同じ（**`www.aspath-life.com`** が正）

### 5-6. 数日後の確認

- [ ] Search Console で **`/blog/` のエラーが出ていない**
- [ ] インデックスが `/column/` に移っている
- [ ] 旧記事内のGoogleフォーム／LINE URLの扱い（確認事項2・3）

---

## 6. うまくいかないときの戻し方

| 症状 | 対処 |
|---|---|
| **全ページが崩れた** | 外観 → テーマ → **旧テーマに戻す**。それだけで元の見た目に復帰します |
| ページが白紙 | スラッグの綴り違い。§3-4の表と1文字ずつ照合 |
| ページを開くとコラム一覧 | 表示設定の「投稿ページ」に誤ったページを指定している |
| コラムの画像が出ない | アップロード年月の違い。§2-1のとおり作り直して貼り直す |
| フォームで「送信が無効」 | §4-6のキャッシュ除外 |
| 取り返しがつかない | §2-3のバックアップから復元 |

> 💡 **方式Bの最大の利点はここです。** テーマを戻すだけで見た目は即座に元に戻ります。
> DBを上書きしていないので、投稿や設定が壊れることはありません。

---

## 7. 当日の持ち物チェック（印刷して使えます）

```
□ 本番のバックアップを取得済み
□ aspath-theme.zip（最新）
□ ★アップロード用_投稿画像18点/
□ ★コラム本文_貼り付け用_本番/（3本）
□ ★お知らせ_営業開始キャンペーン_インポート用.xml
□ ★メディア登録_alt一覧_18点.md
□ 本番の管理画面にログインできる
□ LINE Official Account Manager にログインできる
□ 本番の投稿が6本のままか再確認した
```

---

## 8. 山口様に確認が必要なこと

| # | 内容 |
|---|---|
| 1 | **切替の実施日時**（サイトが40分止まります。訪問の少ない時間帯を希望） |
| 2 | 旧記事内の**Googleフォーム**（`forms.gle/sV6P2hbwowZS1quA7`）を限定URLに差し替えてよいか |
| 3 | **公式LINEのURLが2種類**。旧記事内は `lin.ee/t53HwKM`、新サイトは `lin.ee/5kiH4i3`。どちらが現役か |
| 4 | 「**パーキンソン病の振るえとは？**」を新デザインに揃えるか |
| 5 | フッターの「**English**」リンク（英語ページが無く404になる）の扱い |
| 6 | 初回体験フォームの通知先が **`aspathlife@gmail.com`** で合っているか |
