# ASPATH WordPress 移行 作業手順書
### （ステージング環境構築 → 完全移行）

**対象サイト**：ASPATH（アスパス｜鹿児島・パーキンソン病/脳卒中専門トレーニングスタジオ）
**現行**：静的HTML 一式（`dev-aspath-life-main`）— 全ページ単一HTML＋埋め込み画像＋独自CSS/JS
**目標**：WordPress へ完全移行。**まずステージング環境で構築・検証し、確定後に本番へ切替**
**前提**：WordPress の管理・編集権限あり。**本番サイトは直接編集せず**、ステージングで作業する

---

## 0. 全体方針（最初に決めること）

現行サイトは、独自のCSS/JS（ヒーロースライダー、スクロールで追従するヘッダー、日英切替、フォームの項目出し分け、FAQアコーディオン、料金・アクセス等の作り込み）を多用した“作り込み型”です。デザインを忠実に保つため、次の方針を推奨します。

- **推奨方式：オリジナルテーマ（または既存テーマの子テーマ）化**
  現行のHTML/CSS/JSをそのままテーマ化し、見た目・挙動を100%再現します。デザイン崩れのリスクが最小です。
- **更新頻度の高いコンテンツは編集可能領域に分離**
  コラム/お知らせは「投稿（post）」、料金・キャンペーン・実績・アクセス拠点などは「カスタムフィールド（ACF）」や「再利用ブロック」に載せ、山口様がWP管理画面から更新できるようにします。
- **代替案（参考）**：Elementor等のページビルダーで作り直す方式もありますが、現行の独自挙動（スライダー/日英切替/フォーム分岐）の再現に工数がかかるため、本サイトでは非推奨。

> ✅ **確定**：オリジナルテーマ化で進行（付録B-1）。

---

## 1. ステージング環境の構築

1. **ステージング先を用意**
   - レンタルサーバのステージング機能（エックスサーバー/ConoHa等の「テスト環境」）を利用、または `staging.aspath-life.com` のサブドメイン、またはローカル（Local by Flywheel）で構築。
2. **WordPress をクリーンインストール**
   - WordPress 最新版／PHP 8.1以上／MySQL(MariaDB)／独立したDB。
3. **検索エンジンに出さない設定**
   - 「設定 → 表示設定 → 検索エンジンでの表示を許可しない」にチェック（noindex）。
   - 可能なら **Basic認証** を掛けて第三者アクセスを遮断。
4. **バックアップ体制**
   - 本番・ステージング双方で、作業前に必ずフルバックアップ（DB＋ファイル）。以降も定期取得。

> ✅ **確定**：ホスティングは**エックスサーバー**。**サーバーパネルの公式ステージング機能**で本番を複製し、ステージングで全作業→完了後に「本番へ反映」。手順は付録Eに記載。

---

## 2. WordPress 初期設定

- **パーマリンク**：「投稿名」（`/%postname%/`）に設定。※現行の日本語スラッグURLとの整合は「6. リダイレクト」で対応。
- **不要データの削除**：初期投稿「Hello world」、サンプル固定ページ、未使用テーマ・プラグイン。
- **一般設定**：サイトタイトル／キャッチフレーズ、タイムゾーン（東京）、日付形式、サイト言語＝日本語。
- **プラグイン方針：現行の導入済みプラグインを最大活用（追加は最小限）**。詳細は付録F。
  - SEO：**SureRank SEO（既存）**
  - フォーム：**SureForms（既存）**（条件分岐で「ご相談の種類」別の項目出し分け・自動返信）
  - セキュリティ：**CloudSecure WP Security（既存・Xserver）**
  - 画像最適化：**Optimole（既存）**
  - キャッシュ：**Super Page Cache（既存）**（反映後にパージ）
  - ステージング/バックアップ：**WP STAGING ／ All-in-One WP Migration（既存）**
  - 迷惑メール対策：**Akismet（インストール済・要有効化）** ＋ reCAPTCHA
  - メール到達性：**WP Mail SMTP（新規追加を推奨）** … Gmail宛の自動送信対策
  - 多言語プラグインは**不要**（日英はJS方式を維持）
  - 予約：**LatePoint（既存）** … 継続利用可否は付録F-A参照

> ✅ **確定**：**現行のJS方式（data-en）を維持**（付録B-3）。多言語プラグインは導入しません。

---

## 3. デザイン移行（テーマ化）

現行の見た目・挙動をテーマに落とし込みます。

1. **テーマ雛形を作成**（オリジナル or 子テーマ）
   - `style.css`（テーマ情報＋現行の全CSS：`:root` のカラー変数一式を含む）
   - `functions.php`（CSS/JSの読み込み＝enqueue、メニュー登録、アイキャッチ有効化、ACF連携等）
2. **共通パーツを分離**
   - `header.php`：ヘッダー（ロゴ／グローバルメニュー／ご予約・公式LINEボタン／SNS）、モバイルドロワー、**スクロール追従＋ひょっこり表示の挙動**、**日英切替**。
   - `footer.php`：フッター（会社情報／サポートナビ／サブフッターの法的リンク＋English）。
3. **ページテンプレート**
   - `front-page.php`：TOP（ヒーロースライダー、メッセージ、選ばれる3つの理由、サービス、お悩み、料金導線、実績・受賞歴、SNS、FAQ導線 等）。
   - `page-about.php` / `page-price.php` / `page-access.php` / `page-contact.php` / `page-faq.php` / `page-privacy.php` / `page-tokushoho.php` / `page-sitemap.php`。
4. **JavaScript を移植（enqueueで読み込み）**
   - ヒーロークロスフェードスライダー、ヘッダー挙動、フォームの「ご相談の種類」による項目出し分け、FAQアコーディオン、実績「もっと見る」、日英切替、タブレット時のヘッダー自動収まり。
5. **画像の扱い（重要）**
   - 現行は画像を **base64でHTMLに直接埋め込み**。WordPressでは **メディアライブラリへ移行**し、`<img>`で参照する方式に変更（保守性・表示速度・キャッシュ効率が向上）。
   - ロゴ／ヒーロー写真／アクセス写真／コラム図版／お悩みイラスト／賞アイコン 等をアップロードして差し替え。

> ✅ **確定**：**メディアライブラリ方式**に切り替え（付録B-4）。現行のbase64埋め込み画像を書き出して登録します。

---

## 4. コンテンツ移行

1. **固定ページ（Pages）として作成**
   - TOP／ASPATHについて／プランと料金／交通アクセス／お問い合わせ／よくある質問／プライバシーポリシー／特定商取引法に基づく表記／サイトマップ。
   - 「設定 → 表示設定 → ホームページ」で TOP を固定表示に設定。
2. **コラムは「投稿（Posts）」化**
   - カテゴリ「コラム」を作成し、パーキンソン病コラム（`パーキンソン病とアスパスの歩み方`）等を投稿として登録。
   - **アイキャッチ画像**を設定 → 一覧・TOP・記事本ページで自動表示。出典リンク・図版も本文に配置。
   - 脳卒中コラムは**内容確定後に投稿**（現状はアイキャッチ枠のみ）。
3. **編集しやすさの設計（山口様が更新する箇所）**
   - 料金・キャンペーン（半額4,400円 等）、実績・受賞歴、アクセス3拠点、代表挨拶、SNSリンク などは **ACF（カスタムフィールド）** か **再利用ブロック** にして、テンプレート改修なしで更新可能に。
4. **メニュー・ウィジェット**
   - グローバルメニュー、フッターメニュー、サブフッター（法的リンク＋English）をWPメニューで管理。

---

## 5. フォーム移行（お問い合わせ）

現行のNetlifyフォームを、WordPress用フォームへ置き換えます。

- **プラグイン**：Contact Form 7（＋Conditional Fields）または WPForms。
- **再現する仕様**：
  - 「ご相談の種類」の選択に応じた **項目の出し分け**（初回体験のときだけ生年月日・住所・どなた・診断名などを表示）。
  - メール・電話・「知りましたか？」・メッセージは**任意**（現行仕様に合わせる）。
  - 送信先メールアドレス、**自動返信メール**、件名の出し分け（【初回体験】／【料金相談】等）。
  - スパム対策：reCAPTCHA v3 ＋ Akismet。
- **テスト送信**で受信・自動返信・迷惑メール振り分けを必ず確認。

### 5-2. 自動返信メール文面（お客様向け・ドラフト）

> `[  ]` はフォーム項目の差し込みタグ（Contact Form 7 等の `[your-name]` に相当）。実装時に各項目名へ置き換えます。

**件名**：【ASPATH】お問い合わせありがとうございます（自動返信）

**本文**：
```
[お名前] 様

この度は、ASPATH（アスパス）へお問い合わせいただき、
誠にありがとうございます。

以下の内容でお問い合わせを受け付けいたしました。
━━━━━━━━━━━━━━━━━━━━
■ ご相談の種類：[ご相談の種類]
■ お名前：[お名前]（[フリガナ]）
■ メールアドレス：[メールアドレス]
■ お電話番号：[お電話番号]
■ お問い合わせ内容：
[メッセージ]
━━━━━━━━━━━━━━━━━━━━

内容を確認のうえ、担当者より24時間以内にご連絡させていただきます。
いましばらくお待ちくださいませ。

※本メールは自動送信です。ご返信いただいても対応できかねます。
※2日以上経っても連絡がない場合は、お手数ですが公式LINE、
　または下記メールアドレスまでご連絡ください。

────────────────────
ASPATH（アスパス）
パーキンソン病・脳卒中専門 トレーニングスタジオ｜鹿児島
Email：aspathlife@gmail.com
公式LINE：https://lin.ee/5kiH4i3
────────────────────
```

### 5-3. 通知メール文面（スタジオ受信用・ドラフト）

**件名**：（種類別）【初回体験】ASPATH お申し込み／[お名前] 様
※種類により件名を出し分け：【サービス内容の質問】【料金相談】【取材・お仕事のご依頼】【その他】

**本文**：
```
お問い合わせがありました。

■ ご相談の種類：[ご相談の種類]
■ お名前：[お名前]（[フリガナ]）
■ メールアドレス：[メールアドレス]
■ お電話番号：[お電話番号]
（初回体験の場合）
■ 生年月日：[生年月日]
■ ご住所：[ご住所]
■ どなたについて：[どなた]
■ 診断名：[診断名]／[診断名その他]
■ 困っていること：[困っていること]
■ ASPATHを知ったきっかけ：[知りましたか]
■ メッセージ：
[メッセージ]

送信日時：[送信日時]
```

> 文面はたたき台です。ご希望のトーン・追記（キャンセルポリシー、アクセス案内 等）があれば調整します。

---

## 6. SEO・構造化データ・リダイレクト

- **各ページの title / meta description / OGP** を設定（現行の内容を移植）。
- **構造化データ（JSON-LD：LocalBusiness / FAQPage 等）** を移植（アクセス3拠点・FAQ）。
- **XMLサイトマップ**（SEOプラグインが自動生成）、`robots.txt`。
- **リダイレクト設計（301）**：
  - 現行 aspath-life.com の既存URL（日本語スラッグ含む）と、新WPのURLが変わる場合は **301リダイレクト**で旧→新をつなぎ、SEO評価・被リンクを引き継ぐ。
  - Redirection プラグイン等で管理。

---

## 7. 検証（ステージングで実施）

**表示・挙動**
- 全ページ表示、**レスポンシブ（PC／タブレット／スマホ）**、崩れ確認。
- ヘッダーのスクロール追従・縮小、ヒーロースライダー、日英切替、FAQ開閉、実績「もっと見る」、お悩み/選ばれる理由 等の挙動。

**機能**
- お問い合わせフォーム：種類別の項目出し分け、必須/任意、送信・自動返信・受信テスト。
- 公式LINE／ご予約 ボタンのリンク、SNSリンク（Instagram/YouTube/Facebook/LINE）、コラム出典リンク。

**品質**
- リンク切れチェック、画像表示、表示速度（PageSpeed）、クロスブラウザ（Chrome/Edge/Safari/スマホ）。

> チェックリストは巻末に用意しています。

---

## 8. 本番移行（切替）

1. **本番のフルバックアップ**（切り戻し用）。
2. **ステージング → 本番へ反映**
   - 移行プラグイン（**All-in-One WP Migration** / **Duplicator**）でエクスポート→本番へインポート。
   - または手動（テーマ・プラグイン・DB・メディアを反映）。
   - **URL置換**（ステージングURL→本番URL）を漏れなく実施（Search-Replace）。
3. **公開設定に戻す**：noindex解除、Basic認証解除。
4. **ドメイン・SSL**：独自ドメイン割当、SSL（https）有効化、http→httpsリダイレクト。
5. **本番で最終確認**（フォーム受信テストを含む）。
6. **Google Search Console / Analytics** 設定、サイトマップ送信。

---

## 9. 公開後の運用

- **自動バックアップ**（日次/週次）を設定。
- WordPress・テーマ・プラグインの**定期更新**（ステージングで先に検証してから本番反映）。
- **山口様への操作レクチャー**：投稿（コラム）の追加、料金/キャンペーン等（ACF/再利用ブロック）の更新、画像差し替え、フォーム受信確認の手順。

---

## 付録A：作業チェックリスト

- [ ] ステージング環境構築（noindex／Basic認証／バックアップ）
- [ ] WP初期設定・必要プラグイン導入
- [ ] テーマ化（header/footer/各テンプレート/CSS/JS）
- [ ] 画像をメディアライブラリへ移行
- [ ] 固定ページ作成（全ページ）
- [ ] コラムを投稿化（アイキャッチ設定）
- [ ] 編集領域の設計（ACF/再利用ブロック）
- [ ] フォーム構築（項目出し分け・任意設定・自動返信・スパム対策）
- [ ] SEO/OGP/構造化データ/サイトマップ
- [ ] 301リダイレクト設計
- [ ] 全ページ検証（表示・レスポンシブ・機能・速度・クロスブラウザ）
- [ ] 本番バックアップ → 反映 → URL置換
- [ ] noindex/Basic認証解除・SSL・独自ドメイン
- [ ] Search Console/Analytics・最終フォームテスト
- [ ] バックアップ自動化・操作レクチャー

---

## 付録B：確定事項（ご回答反映済み）

| # | 項目 | 決定 |
|---|------|------|
| 1 | デザイン移行方式 | **オリジナルテーマ化**で進行 |
| 2 | ステージング環境 | 山口様が契約中の **aspath-life.com のWordPress（管理画面）内のステージング環境**で構築。全作業完了後に**本番環境へ差し替え** |
| 3 | 日英切替 | **現行のJS方式（data-en）を維持** |
| 4 | 画像 | **メディアライブラリ方式** に移行 |
| 5 | フォーム自動返信 | **文面込みで用意**（本手順書 5-2 にドラフト掲載）。送信先メールアドレスのみ別途ご共有ください |
| 6 | 既存ブログ/コンテンツ | **現在制作中のサイト（dev-aspath-life-main）が"正"**。WordPressには**新規構築**（旧サイト内容は引き継がず、新サイトで置き換え） |
| 7 | 公開時期 | **本公開は9月予定**。ただし **8月前（7月末）までに「いつでも公開できる状態」** を目標 |

> ✅ **送信先メール確定**：`aspathlife@gmail.com`

---

## 付録C：スケジュール（目安）

| 時期 | 内容 |
|------|------|
| 7月中 | ステージング構築・オリジナルテーマ化・全ページ移植・画像メディア化・フォーム構築・コラム投稿化 |
| **7月末** | **「いつでも公開できる状態」に到達**（全検証完了・本番差替の準備完了） |
| 8月 | 予備期間（微修正・追加コラム反映・最終確認・関係者レビュー） |
| **9月** | **本番公開（本番環境へ差し替え・DNS/SSL・noindex解除・Search Console登録）** |

> 8月前に公開可能状態を確保するため、7月は「構築＋検証」に集中し、8月は調整・予備に充てる想定です。

---

## 付録D：旧サイトからの引き継ぎ（新規構築でも実施すること）

新規構築（付録B-6）でも、SEO資産を守るため以下は実施します。

- 旧 aspath-life.com に既存の**公開URL（例：パーキンソン病コラムの日本語スラッグURL 等）**がある場合、新サイトの対応ページへ **301リダイレクト**。
- 旧サイトの**インデックス済みURL**を洗い出し、新URLへの対応表を作成。
- 公開後、**Search Console でサイトマップ再送信**・カバレッジ確認。

---

---

## 付録E：サーバー環境（確定・WordPress サイトヘルスより）

| 項目 | 値 | 移行上のポイント |
|------|-----|------------------|
| Webサーバー | **Apache** | **.htaccess による 301リダイレクト**が利用可（旧URL引き継ぎに有利） |
| PHP | **8.3.30**（64bit）／fpm-fcgi | 最新系。オリジナルテーマ・主要プラグインと互換問題なし |
| メモリ上限 | **1G** | 余裕あり。プラグイン・画像処理も安定 |
| PHPタイムリミット | 300秒 | 移行/インポート処理に十分 |
| アップロード上限／POST上限 | **各1G** | ステージング⇄本番の移送（All-in-One WP Migration 等）が容易。画像一括アップも余裕 |
| Imagick | **利用可** | 画像のリサイズ／WebP生成が高品質 |
| プリティパーマリンク | 対応 | `/%postname%/` 設定OK |
| OPcache | 有効（現在“満杯”） | **反映後は OPcache/キャッシュのクリア**を手順に含める |
| robots.txt | WP動的生成 | 公開前は noindex、公開時に解除 |
| サーバ時刻 | JST（UTC+9） | タイムゾーン整合OK |

**総評**：移行に十分な良環境です。特に **Apache（.htaccessリダイレクト可）＋大きめのアップロード/メモリ上限** は、ステージング構築と本番差し替えに有利。

### メール送信について
- 送信先：**`aspathlife@gmail.com`（確定）**
- Gmail宛の自動送信（自動返信・通知）は**迷惑メール判定を受けやすい**ため、**WP Mail SMTP プラグイン＋認証済み送信元**での送信を推奨。最低限、到達テスト（受信トレイに届くか）を実施します。

### ホスティング：エックスサーバー（確定）
SSL証明書のコモンネームが `*.xserver.jp` のため、**エックスサーバー**と確定。エックスサーバーには**公式のステージング機能**があるため、これを用いて構築→本番反映します。

**ステージング作成〜本番反映の流れ（Xserver）**
1. **サーバーパネル →「WordPress」→「WordPress設定」** → 対象ドメイン `aspath-life.com` を選択
2. **「ステージング」タブ →「ステージング環境を作成する」** → 本番のコピー（テスト用URL）が自動生成
3. ステージング環境にログインし、**オリジナルテーマ化・全ページ・画像メディア化・フォーム・コラム投稿・SEO** を構築（本手順書 2〜7）
4. 検証完了後、**「本番環境へ反映する（マージ）」** で本番へ反映
   - ⚠ 反映前に**本番のフルバックアップ**を取得（Xserverの「バックアップ」機能 または UpdraftPlus）
   - ⚠ 反映後は **OPcache／Xアクセラレータ（サーバーキャッシュ）をクリア**

**エックスサーバー固有の設定**
- **PHPバージョン**：8.3系を維持（サーバーパネル →「PHP Ver.切替」）。
- **無料独自SSL**：本番ドメイン `aspath-life.com` にSSLが適用されているか要確認（今回の証明書CNが `*.xserver.jp` だったため、独自ドメインSSLの適用状況をチェック）。未適用なら「SSL設定 → 独自SSL設定追加（無料Let's Encrypt）」で発行し、`https` 常時化。
- **.htaccess**：ファイルマネージャ or FTPで編集可 → **301リダイレクト**を設置（旧URL→新URL）。
- **簡単バックアップ**：自動バックアップの保持を確認（不足なら UpdraftPlus で補完）。

> エックスサーバーのステージングは**サーバー単位のコピー**のため、プラグイン差分等も含めて安全に検証→反映できます。プランによりUI表記が異なる場合がありますが、基本フローは同じです。

---

---

## 付録F：現行WordPressの実態と方針への反映（システム情報より）

### 現状サマリー
- **WP 7.0.2 / PHP 8.3.30 / MariaDB 10.5 / Apache（エックスサーバー）**、HTTPS有効、日本語、`/%postname%/`。
- **総容量 約866MB**（uploads 147MB／themes 55MB／plugins 157MB／DB 31MB）。ユーザー1名。
- **現行テーマ：Astra 4.13.2 ＋ Spectra／Otter（Gutenbergブロック）＋ Starter Templates**（＝ノーコード寄りの構成）。
- 予約：**LatePoint**（予約/アポイント）導入済み。
- フォーム：**SureForms** 導入済み。SEO：**SureRank SEO** 導入済み。
- 画像最適化：**Optimole**（自動最適化/CDN）。キャッシュ：**Super Page Cache**。セキュリティ：**CloudSecure WP Security（Xserver）**。
- ステージング/バックアップ：**WP STAGING** ＋ **All-in-One WP Migration** 導入済み。
- 未使用：Lightning系・Twenty系・VK FullSite Installer・Hello Dolly など。

### 方針への反映（重要）
1. **オリジナルテーマ化＝Astra からの置き換え**。現行Astra/ブロックの見た目は破棄し、`dev-aspath-life-main` の完成デザインを新テーマで再現します。既存の投稿（コラム等）は内容が残り、新テーマの体裁で表示されます。
2. **既存プラグインを最大活用（追加を最小化）**：
   - フォーム → **SureForms（既存）** で「ご相談の種類」別の**条件分岐（項目出し分け）**・任意設定・**自動返信**を実装（Contact Form 7の追加は不要）。
   - SEO → **SureRank（既存）** で title/description/OGP/構造化データ/サイトマップ。
   - ステージング → **WP STAGING（既存）** または **エックスサーバー公式ステージング**。
   - 画像 → **Optimole（既存）** が自動最適化。現行のbase64画像はメディアライブラリへ登録すればOptimoleが処理。
   - キャッシュ → **Super Page Cache（既存）**。反映後にパージ。
   - バックアップ/移送 → **WP STAGING / All-in-One WP Migration（既存）**。※総容量866MBのため、AIOWPMの**インポート上限**に注意（ステージング反映は WP STAGING or Xserver機能で行えば容量問題なし）。
3. **メモリ**：`WP_MEMORY_LIMIT` が 40M と低め。移行/一括処理の安定化のため **256M** へ引き上げ推奨（wp-config.php）。
4. **不要テーマ/プラグインの整理**：Lightning系・Twenty系・VK・Hello Dolly など未使用分は、本番反映前に整理（軽量化/セキュリティ）。

### 確定事項（着手前）
- ✅ **A. 予約導線 確定**：**移行先（dev）のデザインに統一**。「ご予約はこちら」は **お問い合わせフォーム（SureForms）** に繋ぎ、フォーム内「ご相談の種類＝初回体験を申し込みたい」で受け付ける（＝devサイトの流れを再現）。現行 **LatePoint の予約ウィジェットは前面では使用しない**。
  - ⚠ 留意：現在 LatePoint で**カレンダー日時予約**を運用中の場合、フォーム化により「日時を選んで予約」機能はなくなります（日程はフォーム送信後に個別調整＝devサイトの想定どおり）。この方針で進めます。LatePointはプラグインとして残置/停止のいずれも可。
- ✅ **B. フォーム 確定**：現行の独自JSフォーム（種類別の項目出し分け・任意設定・自動返信）を **SureForms で再現**（＝devデザインに統一）。
- ✅ **C. テーマ 確定**：テーマは**移行先のWordPressサイト（ステージング）上で確定**させる。オリジナルテーマを構築し、**Astra から置き換え**（dev-aspath-life-main の完成デザインを新テーマで再現）。

> **A・B・C すべて「移行先（dev）の完成デザインに統一」で確定。** 現行のAstra／LatePoint／ブロック構成の見た目は破棄し、devサイトを"正"として新テーマ・新フォームで再現します。

---

*本手順書は、現在制作中の静的HTML（dev-aspath-life-main）を"正"として WordPress へ新規構築・移行する前提でまとめています。付録B・E・F の確定事項に沿って各工程を具体化して進めます。*
