# ASPATH サイト → WordPress 移行 段階手順書

対象：現在制作中の静的サイト（`dev-aspath-life-main`｜全14ページ・日英切替・base64埋め込み画像・お問い合わせフォーム）
移行先：山口様が公開中のWordPressドメイン
方針：**現行の本番サイトを止めず、ステージング（テスト環境）で完成させてから切替える**

---

## 大前提（最重要）

1. **本番のWordPressをいきなり触らない。** 必ず「バックアップ」と「ステージング（複製したテスト環境）」で作業する。
2. **山口様の公開中サイトを一度フルバックアップする**（データベース＋ファイル）。これが最終的な保険になる。
3. 作業は「準備 → ステージング構築 → 移植 → 検証 → 本番切替 → 公開後チェック」の順で、各段階を終えてから次へ進む。

---

## フェーズ0：準備・バックアップ（着手前に必ず）

- [ ] 現行WordPressの**フルバックアップ**を取得（推奨プラグイン：UpdraftPlus / All-in-One WP Migration のいずれか）。
- [ ] バックアップは**別の場所（PC・Googleドライブ等）にもダウンロード**して二重保管。
- [ ] 契約中のレンタルサーバー管理画面にログインできることを確認（サーバー会社・プラン・PHPバージョンを控える）。
- [ ] ドメイン・DNSの管理画面にアクセスできることを確認。
- [ ] 現行サイトのURL構成（トップ、下層ページのパス）を一覧化しておく（後述のリダイレクトで使う）。

---

## フェーズ1：移行方式を決める（この案件のおすすめ）

現行サイトは「完成度の高い静的HTML」です。作り直さずに活かすのが最短・低リスクです。方式は3つ。

### ★推奨：方式A「固定ページ＋フルワイド（空白）テンプレート」
- WordPressの**子テーマ**を作り、ヘッダー/フッターを出さない**全幅の白紙テンプレート**を用意。
- 各HTMLページの中身を、その白紙テンプレートの固定ページとして表示する。
- 既存のデザイン・日英切替JS・ローディングをそのまま活かせる。
- page builder（Elementor等）を使う場合は「Canvas（キャンバス）」テンプレートでも同じことが可能。

### 方式B「静的ファイルをサブディレクトリで併存」（一番簡単・暫定向き）
- WordPressと同じサーバーの `/lp/` などにHTMLを置いて直接配信（例：`ドメイン/lp/`）。
- WordPress本体には手を入れないため最速だが、WPの管理画面からは編集できない。まず見せたい・急ぎのときの暫定策。

### 方式C「WordPressテーマとして作り直し」（本格・長期運用向き）
- ヘッダー/フッター/メニューをWPの共通パーツ化し、記事（お知らせ・コラム）をWP投稿で管理。
- 運用は最も楽になるが工数大。まずはA/Bで公開し、余力ができたらCへ、が現実的。

> **結論：まずは「方式A」で移植 →** 将来お知らせ/コラムをWP投稿で回したくなったら方式Cへ段階移行、を推奨します。

---

## フェーズ2：ステージング（テスト環境）構築

- [ ] 利用中サーバーに**ステージング機能**があれば有効化（エックスサーバー/ConoHa/mixhost等は標準搭載）。
- [ ] 無い場合は、サブドメイン（例：`stg.ドメイン`）に現行WPを複製（All-in-One WP Migration でエクスポート→インポート）。
- [ ] ステージングは**検索エンジンにインデックスさせない**設定にする（WP管理画面 → 設定 → 表示設定 →「検索エンジンがサイトをインデックスしないようにする」にチェック）。
- [ ] ステージングにBasic認証（パスワード保護）をかけられればなお安全。

---

## フェーズ3：画像・アセットの整理（推奨）

現行HTMLは画像をbase64で埋め込んでおり1ファイルが重め。WordPressでは**画像をメディアライブラリに移すと表示が速く・管理も楽**になります（任意だが推奨）。

- [ ] base64画像を書き出し、WordPressの**メディアライブラリ**にアップロード。
- [ ] HTML内の `data:image/...;base64,...` を、アップロード後の画像URL（例：`/wp-content/uploads/2026/07/hero.webp`）に置換。
- [ ] Googleフォント・CSSは現状のまま（`<head>` の読み込みを維持）。
- [ ] favicon・OGP画像もメディアに登録し、パスを差し替え。

> 急ぎで公開したい場合はbase64のままでも動作します。**「まず公開 → あとで画像を軽量化」**の順でもOK。

---

## フェーズ4：ページの移植（方式A手順）

1. [ ] 子テーマを作成（`style.css` と `functions.php` のみの最小構成でOK）。
2. [ ] 全幅・白紙のページテンプレート（例：`page-fullwidth.php`）を追加し、`the_content()` だけを出力（ヘッダー/フッター/サイドバー無し）。
3. [ ] 固定ページを14ページ分作成（トップ／ASPATHについて／スタッフ／料金／アクセス／FAQ／コラム一覧／コラム2種／お問い合わせ／サイトマップ／プライバシー／特商法）。
4. [ ] 各固定ページで**テンプレートを「全幅・白紙」に設定**し、対応HTMLの `<body>` 内側をコピペ（`<html>`/`<head>` は不要、CSS・JSは `<head>` 相当をテンプレート側 or ページ先頭に読み込ませる）。
5. [ ] トップを「ホームページ」に設定（設定 → 表示設定 → ホームページ＝固定ページ「トップ」）。
6. [ ] パーマリンク（URL）を現行と揃える（設定 → パーマリンク）。旧URLと変わる場合はフェーズ7でリダイレクト。

---

## フェーズ5：メニュー・共通パーツ・日英切替

- [ ] グローバルメニュー／フッター／ドロワーは各ページHTMLに内包済みのため、まずはそのまま表示でOK。
- [ ] **日英切替**は現行の独自JS（localStorageで言語保持）がそのまま動きます。**Polylang/WPML等の多言語プラグインは導入しない**（現行の切替と競合するため）。将来WP流の多言語にしたい場合のみ方式Cで再設計。
- [ ] SNSリンク（LINE `lin.ee/5kiH4i3`／Instagram／YouTube／Facebook）が正しいURLか最終確認。

---

## フェーズ6：お問い合わせフォーム（要対応）

静的サイトのフォームは**送信先の仕組み（メール送信）がない**ため、WordPress側で送信できるようにします。

- [ ] **Contact Form 7** または **WPForms** を導入。
- [ ] 現行フォームの項目（ご相談の種類で詳細/簡易が切替わる設計）を再現。
- [ ] 送信先メール・自動返信・**2日以上返信がない場合はLINE/メール**の案内文を設定。
- [ ] 迷惑メール対策（reCAPTCHA v3 か Akismet）を有効化。
- [ ] 送信テスト（実際に自分宛に届くか）を必ず確認。

---

## フェーズ7：SEO・リダイレクト・地図等

- [ ] 各ページの `title` / `meta description` / OGP を確認（現行HTMLに記述済み。SEOプラグインYoast/SEO SIMPLE PACK使用時は重複しないよう調整）。
- [ ] `sitemap.xml` を生成（SEOプラグイン or XML Sitemap系）し、Google Search Consoleへ登録。
- [ ] **旧URL → 新URL のリダイレクト**設定（プラグイン「Redirection」）。URL構成を変えないなら不要。
- [ ] Googleマップ・外部埋め込みが正しく表示されるか確認。
- [ ] アクセス解析（Googleアナリティクス等）のタグを設置。

---

## フェーズ8：検証（ステージングで総点検）

- [ ] **全14ページ**の表示（レイアウト崩れ・画像抜けが無いか）。
- [ ] **日英切替**：ボタンで切替＆ページ移動しても言語が保持されるか。
- [ ] **スマホ表示**（iPhone/Android実機 or 幅375/390/414）。
- [ ] **ローディング画面**が約3秒で消えるか。
- [ ] **リンク切れ**（内部リンク・SNS・電話・メール）。
- [ ] **お問い合わせ送信**が実際に届くか（詳細/簡易 両方）。
- [ ] 表示速度（PageSpeed Insights）を確認、base64のままなら画像軽量化を検討。

---

## フェーズ9：本番切替（公開）

- [ ] 切替直前に**本番を再度フルバックアップ**。
- [ ] 公開はアクセスの少ない時間帯に。
- [ ] 切替方法（サーバー環境により選択）：
  - ステージング機能利用時：管理画面の**「本番へ反映（Push to Live）」**を実行。
  - 手動複製時：ステージングを All-in-One WP Migration で書き出し、本番へインポート（※本番の既存データを上書きするため、直前バックアップ必須）。
- [ ] 公開後、ステージングの**「インデックスさせない」設定を本番では必ず解除**（設定 → 表示設定のチェックを外す）。
- [ ] SSL（https）が全ページで有効か、混在コンテンツ警告が出ていないか確認。

---

## フェーズ10：公開後チェック（当日〜数日）

- [ ] 主要ページ・スマホ・日英・フォーム送信を本番で再確認。
- [ ] Google Search Console でサイトマップ送信・インデックス状況を確認。
- [ ] 数日はアクセス解析とお問い合わせ着信を見守る。
- [ ] 問題があれば、フェーズ0/9のバックアップから**即ロールバック**できる状態を保つ。

---

## つまずきやすいポイント（この案件特有）

- **フォームは静的のままでは送信できない** → 必ずWP側（CF7/WPForms）で再構築。
- **多言語プラグインは入れない** → 現行の独自切替JSと競合する。
- **base64画像で1ファイルが重い** → 公開は可能。速度が気になれば画像をメディアへ移して軽量化。
- **URLが変わるとSEO評価が途切れる** → できる限り旧URLを踏襲し、変わる場合はリダイレクト必須。
- **ヘッダー/フッターが各ページ内に内包**されている → 方式A（白紙テンプレ）ならそのまま活きる。将来の一括修正を楽にしたいなら方式Cで共通パーツ化。

---

## 最短ルート（急ぐ場合のまとめ）

1. 本番フルバックアップ →
2. ステージング複製（noindex）→
3. 方式Aで14ページ移植（画像はまずbase64のまま）→
4. フォームをCF7で用意 →
5. 検証（表示・日英・スマホ・送信）→
6. 直前バックアップ → 本番反映 → noindex解除・SSL確認 →
7. 公開後チェック。

余裕ができたら「画像の軽量化」「お知らせ/コラムのWP投稿化（方式C）」を段階的に。
