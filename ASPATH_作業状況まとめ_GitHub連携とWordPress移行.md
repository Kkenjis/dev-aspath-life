# ASPATH 制作 作業状況まとめ（GitHub連携・WordPress移行）

最終更新：2026年8月4日（数日ぶりに再開）

このドキュメントは、ASPATH（アスパス）サイト制作のこれまでの作業内容と、WordPress移行の進捗を一元的にまとめたものです。

---

## 0. 全体像・現在地

- **開発の正（ソース・オブ・トゥルース）** … `dev-aspath-life-main`（静的HTML 14ページ＋ `images/`）。ここを編集し、GitHubで管理。
- **公開先** … 現行サイトは `aspath-life.com`（WordPress）。**今作っているdevデザインへ全面リニューアル移行中**。
- **移行方式** … Xserver上に**ステージング環境**を構築 → **オリジナルテーマ化** → 全作業完了後に本番へ差し替え。**切替は9月予定／8月前には公開可能な状態**を目標。
- **現在** … dev側のデザイン調整を継続しながら、WordPress移行の土台（フェーズ0〜3）を構築済み。

---

## 1. GitHub連携

| 項目 | 内容 |
|------|------|
| リポジトリ | `https://github.com/Kkenjis/dev-aspath-life`（ブランチ `main`） |
| ローカル | `dev-aspath-life-main` フォルダで `git init` 済み・`origin` 設定済み |
| 追跡対象 | サイト本体のみ（13 HTML ＋ `images/`30点 ＋ README・.gitignore／計45ファイル・約2.3MB） |
| 除外（.gitignore） | 作業バックアップ `*.bak_*`、大容量素材フォルダ、録画mp4、`_wp移行素材/`、`wp完全移行/` 等 |
| コミット総数 | **34コミット**（下記「作業ログ」参照） |

### push方法（変更を反映するとき）
1. **GitHub Desktop**：リポジトリを開く → 右上「**Push origin**」（または Repository → Push）
2. または Git CLI：フォルダ内で `git push origin main`
   - ※初回はforce必要だったが、以降は通常push（fast-forward）でOK
3. Gitが未導入なら [git-scm.com/download/win](https://git-scm.com/download/win) または GitHub Desktop を導入

### 運用ルール
- **dev変更は「コード＋画像をセットで即コミット」**（未コミットを残さない）。pushはご自身で実行。
- GitHub Pages で確認する場合は Settings → Pages → `main / (root)`。

---

## 2. dev側 作業内容（完了分）

### 2-1. 画像のファイル化・命名統一
- HTML埋め込みの**base64画像56件を外部ファイル化**し `images/` へ集約。
- **命名を英語・カテゴリ接頭辞に統一**（`logo-` `hero-` `index-` `about-` `access-` `column-pk-` `icon-` `staff-`）。計26点＋今回追加のヒーロー/シーン写真。
- 山口様提供の**オレンジポロ写真**を反映：ヒーロー4スライド／オンライン・カウンセリング写真／代表ポートレート。

### 2-2. ロゴ・ヘッダー
- TOPロゴ：**藍色ロゴ＋ヒーロー上は白もや（白グロー）**。スクロール時は通常表示。
- ヘッダー整理：**YouTube/Instagramを撤去**しヒーロー右の縦SNSへ移設。**「ご予約はこちら」を削除**。**公式LINEボタンを大型化**。**右下に追従する丸LINEボタン**を追加。
- メニュー：中央寄せ・項目間58px・全体を少し右寄せに調整。公式LINEは右側に余白を付けてアピール。
- **全サブページのヘッダーをindex（正）に完全統一**（markup・フォント・サイズ・レイアウト）。ロゴは48px（TOPスクロール時と同サイズ）。

### 2-3. フォント
- 全ページで**Zen Maru Gothic（丸ゴシック・全漢字対応）を読込**し、Kiwi Maruに無い漢字（問/合等）のフォールバックを機能させ「**お問い合わせ**」のフォント不一致を解消。
- 全ページのフォント読込を5ファミリーで統一（Zen Kaku Gothic New欠落を補完）。

### 2-4. レイアウト（TOP以外）
- **目次・カテゴリ（side-toc）を全撤去**、本文を**1カラム中央寄せ（最大900px・50〜70代向け視認性）**。空カラムで右寄りだったページも中央化。
- **オレンジの小英語見出し（NEWS/COLUMN/FOLLOW US/MESSAGE 等）を全17件削除**。

### 2-5. about（ASPATHについて）
- **ASPATH由来のアクロニム変更**：S=Stroke/脳卒中 → **Support/支える**、A=Assist・Active → **Active/活動的**。英字と日本語の並びを全行で縦整列。
- **サービスカードの左色線を削除**、見出し拡大（20-23px）＋藍色アクセントマーカー付与（アスパスらしさ）。写真丸は変更なし。
- ロゴ説明文・ASPATH名の段落などに**改行を整形**（`word-break:auto-phrase`で日本語を文節折り返し＋手動`<br>`）。

### 2-6. 問い合わせ・初回体験フロー（技術対応）
- **contact.html**：ご相談の種類を**2択（初回体験／その他）**に。
  - 初回体験 → フォームを出さず**公式LINE登録を促す**。
  - その他 → メッセージフォーム。
- **trial-entry.html（新規）**：**限定・非公開の初回体験申込フォーム**。ヘッダー等なしの**フォーム単体・最小構成**。noindex・未リンク。**メールを必須化**（申込者へ入力控えを送るため）。
  - フロー：初回体験 → 公式LINE登録 → 山口様のLINEから**限定公開URL** → trial-entryフォーム到達。
  - **原則：限定公開**（推測されにくいスラッグ＋noindex＋未リンク／1本の固定URL）。
  - 送信時に**2通**：山口様への通知メール＋申込者への控えメール。
  - ※メール送信・限定URLの実装は**WordPress（SureForms）側で設定**。

---

## 3. WordPress移行 進捗（フェーズ別）

| フェーズ | 内容 | 状況 |
|---------|------|------|
| **0. 事前準備** | 本番バックアップ（`.wpress`取得済み）／旧URL棚卸し（サイトマップから**全10URL確定**） | ✅ 完了 |
| **1. ステージング作成** | WP STAGINGで `aspath-life.com/development/` を作成。noindex設定・LightStartで第三者遮断 | ✅ 完了 |
| **2. 基本設定** | パーマリンク=投稿名／タイムゾーン=東京／プラグイン確認／WP Mail SMTP導入済／メモリ256M（山口様対応済） | ✅ 完了 |
| **3. オリジナルテーマ構築** | devから生成した `aspath` テーマ一式（`aspath-theme.zip`）を提供済み | ⚠ **要再生成・未有効化**（下記） |
| **4. 画像のメディア移行** | `images/`26点＋一覧・manifest 準備済 | ⏳ 未着手 |
| **5. コンテンツ投入** | 固定ページ（旧スラッグ踏襲 /services/, /blog/ 等）・コラム投稿化 | ⏳ 未着手 |
| **6. フォーム（SureForms）** | 設計書あり（2フォーム構成・初回体験は限定公開＋2通送信） | ⏳ 未着手 |
| **7. SEO・301リダイレクト** | 対応表確定（リダイレクト不要7本＋301が3本） | ⏳ 未着手 |
| **8. 検証** | ステージングでチェックリスト実施 | ⏳ 未着手 |
| **9. 本番反映** | All-in-One WP Migrationでエクスポート→本番インポート（WP STAGING無料版のためPush不可） | ⏳ 公開直前 |
| **10. 公開後運用** | 301監視・URL/SNS更新 等 | ⏳ 公開後 |

### ⚠ 重要：テーマは「再生成」が必要
`aspath-theme.zip` は**ヘッダー統一・フォント・contact/trial-entry・about等の大幅改修の“前”に生成**したものです。現在のdevとズレているため、**WordPress有効化の前にdevから再生成**してください（`_wp移行素材/theme/` の生成手順で再ビルド）。

---

## 4. 移行用アセット（`_wp移行素材/`）

| ファイル/フォルダ | 内容 |
|------------------|------|
| `images/`（26点）＋ `画像一覧.md` ＋ `images_manifest.json` | メディアライブラリ登録用（旧名との対応も保持） |
| `theme/`（`style.css`・`js/`・`README.md`） | オリジナルテーマ用アセット（enqueue手順込み）※要再生成 |
| `aspath-theme.zip` | テーマ配布zip ※旧版・要再生成 |
| `SureForms項目設計表.md` | フォーム設計（**§7に最新の2フォーム構成・限定公開・2通送信を追記済**） |
| `301リダイレクト対応表.md` | 旧→新URL対応（確定版・全10URL） |
| `ASPATH_WordPress完全移行手順書_実行版.md` | フェーズ0〜10の実行マニュアル（正） |

---

## 5. 保留・次のステップ

**山口様の提供待ち**
- お悩み事セクションの差し替え文章（脳卒中除外・パーキンソン特化）
- 選ばれる3つの理由の文章
- 中央駅の写真（天文館は反映済）
- 白色ロゴ（※現状は藍色＋白もやで対応中。要否は再確認）

**制作側の次の候補**
- TOPのサービスカードを about と同じデザインに統一（左線削除・見出し拡大・マーカー）
- 脳卒中表記の全体的な整理（提供文が届き次第）
- **テーマの再生成**（現行devに合わせて `aspath-theme.zip` を作り直し）
- WordPressフェーズ3-6（テーマ有効化 → 画像 → コンテンツ → フォーム）

**WordPress移行後にやること（要点）**
- 初回体験フォームを**限定公開**で作成 → 固定URLを発行 → 山口様へ共有 → **公式LINEの自動応答/リッチメニュー**に掲載
- SureFormsでメール2通（通知＋控え）を設定・**Gmail到達テスト**（WP Mail SMTP）
- 301リダイレクト（Redirection）設定は**本番切替と同時**

---

## 6. 主要ファイルの場所

- サイト本体：`dev-aspath-life-main/*.html` ＋ `images/`
- 移行アセット：`dev-aspath-life-main/_wp移行素材/`
- 議事録：`第3回_ASPATH_打合せ議事録.md` / `第4回_ASPATH_打合せ議事録.md`
- 本まとめ：`ASPATH_作業状況まとめ_GitHub連携とWordPress移行.md`

---

## 7. その後の追加作業（まとめ初版以降）

| # | 内容 | 状況 |
|---|------|------|
| 1 | contact：**電話番号 090-8298-0433**（タップ発信）をLINEブロックの下・案内文の上に追加 | ✅（事業用電話が整い次第、番号差替） |
| 2 | **脳卒中コンテンツを一旦削除**：脳卒中コラム（`column-stroke.html`本体・TOPカード・news項目・全リンク）＋aboutのトレーニング脳卒中カード。※お悩み文・診断名「脳卒中」欄は残置 | ✅ |
| 3 | index：**代表挨拶の山口様写真を拡大**（写真列 320→380px） | ✅ |
| 4 | **スマホ最適化に着手**：ヒーロー右の縦SNSがスマホで文字に重なる問題を修正（左下の横並びへ再配置）。**変更はスマホ用CSSのみ・PCは現行維持** | 🔄 進行中 |

### スマホ最適化の進め方（現在ここ）
- 全ページ viewport あり・`@media(max-width:760px)` のスマホ用ルールも一通り実装済み。
- **PC版は現行のまま**（変更は `@media` スマホ用に限定）。
- この環境では**スマホ表示を目視できない**ため、実機 or **Chrome の F12→デバイスツールバー（iPhone等）**で確認 → 気になる箇所のスクショを共有 → 個別に最適化、という進め方。

### 現在の構成サマリ
- HTMLページ：**13**（`column-stroke.html`削除・`trial-entry.html`追加）／画像：30点
- Gitコミット：**39**（すべてコミット済み・未pushぶんは GitHub Desktop で Push）

### 直近の優先タスク
1. **スマホ最適化**（実機/デバイスモードで確認しながら各所を調整）← いまここ
2. TOPのサービスカードを about と同じデザインに統一（左線削除・見出し拡大・マーカー）
3. 山口様提供待ち：お悩み文章／選ばれる3つの理由／中央駅写真
4. **テーマ再生成**（現行devに合わせ `aspath-theme.zip` を作り直し）→ WordPressフェーズ3-6
