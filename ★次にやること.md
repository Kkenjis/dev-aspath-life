# ★ 次にやること（ASPATH WordPress移行）

最終更新：2026年8月17日／**このファイルが唯一の入口です。作業を始めるときは必ずここから。**

---

## 0. 作業を始める前に（どのPCでも共通・2分）

```
1. GitHub Desktop で Pull origin        ← 必ず最初に
2. 「テーマを作る.ps1」を右クリック →「PowerShell で実行」
3. _wp移行素材/aspath-theme.zip ができる
```

**実行ポリシーで止まる場合**は、PowerShellを開いて下の1行を貼ってください。

```powershell
powershell -ExecutionPolicy Bypass -File ".\テーマを作る.ps1"
```

> `テーマを作る.ps1` は、Pythonの有無を判定し、生成後に
> **テーマのバージョン／サイズ／PHP・CSS・画像の点数／未Pushコミットの有無**まで表示します。
> 「外観 → テーマ」に出ているバージョンと見比べれば、更新できたかが一目で分かります。
> 失敗したらエラー文をそのまま共有してください。
>
> 従来の `テーマを作る.bat` も残してあります（動作は同じ・表示は簡易）。
> コマンドで実行したい場合は `python build_wp_theme.py` でも同じです。

- ステージング管理画面：`https://aspath-life.com/development/wp-admin/`
- 作業が終わったら **Commit → Push origin**

> **テーマzip・画像コピー・本番エクスポートXMLはgitに入れていません**（容量のため）。
> zipは上のコマンドで再生成、画像は `images/` に全部あります。**手順書とコラム貼り付け用HTMLはgitに入っている**ので、サブPCでもPullすれば揃います。

---

## 1. いまの状況（2026-08-17 実測）

| フェーズ | 状況 |
|---|---|
| 4. テーマ・画像 | ✅ **完了**（テーマ適用済み／画像18点アップ済み） |
| 5. コンテンツ | ✅ **完了** |
| 6. フォーム | ▶ **次はここから** |
| 7. SEO・301 | ⬜ 未着手 |
| 8. 検証・仮公開 | ⬜ 未着手 |

**確認済みで問題なし**

- 固定ページ9本すべて200で正常表示（`/about/` `/services/` `/access/` `/contact/` `/faq/` `/privacy/` `/tokushoho/` `/sitemap/` `/column/`）
- 固定ページ9本すべて `<title>` が日本語（検索結果に英語スラッグが出ない）
- ヘッダーメニュー・フッターリンク（15件）すべて正しい飛び先で200
- 投稿7本すべて公開済み・カテゴリー割り当て済み（コラム4／お知らせ3）
- コラム3本の本文が新デザインに差し替え済み（見出し数・画像点数がdevと完全一致）
- コラム3本のアイキャッチが `column-*-thumbnail.webp` に差し替え済み
- 本文中の画像18点すべて読み込みOK（404ゼロ）
- カテゴリーは **お知らせ／コラムの2つだけ**（`General` `未分類` 削除済み）
- サンプル投稿・サンプルページ削除済み（下書きの投稿ゼロ）
- `/category/info/` にお知らせ3本、`/column/` にコラム4本

> ⚠ **`Customer Cabinet`（下書きの固定ページ）は削除しないでください。** 予約システムが使用します。

> ⚠ **フッター・ヘッダーはWordPressのメニュー機能を使っていません。** テーマ内の固定リンクです
> （`footer.php` `header.php` に `wp_nav_menu` は無し）。**外観→メニュー での設定は不要**。
> リンクを変えたいときは dev のHTMLを直して `build_wp_theme.py` を再実行します。

> 📌 **`English` のリンクだけ飛び先が未作成**（`english.html`）。押すと404。
> 仮公開までに「リンクを外す／英語ページを作る／そのまま」を山口様と決める必要があります。

---

## 2. 残り作業（この順番で）

### ①〜④ ✅ 完了（2026-08-17）

| # | 内容 | 結果 |
|---|---|---|
| ① | コラム3本のアイキャッチ差し替え | ✅ `column-*-thumbnail.webp` に変更済み |
| ② | 固定ページのタイトルを日本語に | ✅ 9本すべて日本語（スラッグは変更なし） |
| ③ | フッターメニューを作る | ✅ **作業不要だった**（テーマの固定リンク。リンク15件すべて200） |
| ④ | サンプル投稿・ページ・カテゴリーを削除 | ✅ 投稿4本／サンプルページ／`General`／`未分類` すべて削除 |

**→ フェーズ5（コンテンツ投入）はここで完了です。**

### ⑤ フェーズ6：初回体験フォーム（限定URL）（40分）★次はここから

**詳細手順 → `_wp移行素材/★初回体験フォーム_限定URL_実行手順.md`**（このファイルを開いて順にどうぞ）

> ✅ **devの既存フォームをWordPressで送信できるように改修済み**（2026-08-17）。
> SureFormsは使いません。見た目・項目・文言はdevのまま、送信の仕組みだけ作り直しました。
> 送信すると **①WP内に保存 → ②山口様へ通知 → ③申込者へ控え（48時間以内と記載）** が動きます。
>
> ⚠ dev版は Netlify 用のフォームだったため、そのままでは
> **画面は正常なのに送信されない**状態でした。修正済みですが、テーマの更新が必要です。

- [ ] テーマを再アップロード（バージョン **`1.1.20260817.2007` 以降**）
- [ ] 固定ページを作成し、テンプレート＝**「初回体験フォーム（限定公開）」**を選択
- [ ] スラッグは推測されにくい文字列（例 `taiken-2026a9k`）／**本文は空のまま**
- [ ] **Super Page Cache の除外に追加**（これを飛ばすと「送信が無効」エラーが出ます）
- [ ] WP Mail SMTP のメールテストが通る
- [ ] **実際に送信テスト** → 緑の完了表示／メール2通／管理画面「初回体験の申込」に記録
- [ ] エラー時に**入力が消えない**ことを確認（お名前を空にして送信）
- [ ] Gmail・キャリアメール両方に届く
- [ ] noindex とサイトマップ除外を確認（テーマ側で自動。SureRank側でも設定）
- [ ] **公式LINEのあいさつメッセージに限定URLを掲載**（LINE Official Account Manager）
- [ ] テスト分の申込を削除

### ⑥ フェーズ7：SEO・301リダイレクト（40分）

詳細 → `_wp移行素材/301リダイレクト対応表.md`

**301を設定する4本**

| 旧URL | 転送先 |
|---|---|
| `/for-parkinsons-disease/` | `/パーキンソン病とアスパスの歩み方/` |
| `/パーキンソン病の方へ/` | `/パーキンソン病とアスパスの歩み方/` |
| `/for-stroke/` | `/about/`（脳卒中コラムを作らない場合） |
| `/blog/` | `/column/` |

- [ ] Redirection プラグイン等で4本を設定
- [ ] 設定後、旧固定ページ3本（`for parkinson's disease` ×2 ／ `for stroke` ／ `パーキンソン病の方へ`）を削除
- [ ] www有無の正規化が旧サイトと同じ（**`www.aspath-life.com`** が正）
- [ ] **noindexは解除しない**（本番反映まで維持）

### ⑦ フェーズ8：検証 → 仮公開（70分）

- [ ] 全ページを **PC・スマホ・タブレット**で確認
- [ ] 重点確認
  - ASPATHについて：特徴カードが**画面幅410px前後のAndroid**で写真と文字が重なっていない
  - ASPATHについて：特徴の3枚でトレーナーが**オレンジのポロシャツ**
  - TOP：「あなたとともに歩む、ASPATH」がスマホで指定どおりの改行
  - TOP：お知らせ・コラムが**各2件**表示
  - お知らせ一覧：**全項目が枠付き**／「重要」が**一番上**で濃い枠
  - プランと料金：フローの写真が**文章の下・中央**、各ブロックの下に**下向き矢印**
  - 全ページ：返信時間の記載が**すべて「48時間以内」**
- [ ] 設定 → 表示設定 → **noindexにチェックが入っている**（最終確認）
- [ ] LightStart のメンテナンスモードを **OFF** → シークレットウィンドウで表示確認
- [ ] 山口様へURLを共有（案内文は `_wp移行素材/仮公開の手順_ステージング限定公開.md`）

---

## 3. 山口様に確認したいこと（保留中）

| # | 内容 |
|---|---|
| 1 | **公式LINEのURLが2種類**。旧記事内は `lin.ee/t53HwKM`、新サイトは `lin.ee/5kiH4i3`。どちらが現役か |
| 2 | 旧記事内の**Googleフォーム**（`forms.gle/...`）を、フェーズ6で作る限定URLに差し替えてよいか |
| 3 | 「**パーキンソン病の振るえとは？**」（8月追加の記事）を新デザインに揃えるか。現状のままでも表示は正常 |
| 4 | **提供待ち**：鹿児島中央町スタジオの写真／お悩み事の差し替え文章／選ばれる3つの理由の文章 |
| 5 | フッターの「**English**」リンク。英語ページが無いため押すと404。リンクを外すか、英語ページを作るか |
| 6 | 初回体験フォームの**通知先メールアドレスは `aspathlife@gmail.com` で合っているか** |
| 7 | 公式LINEの**あいさつメッセージの文面**（案は `★初回体験フォーム_限定URL_実行手順.md` §8-1） |
| 8 | **9月の切り替え方式（A：DBまるごと移行／B：本番で作り直し）**。§4-1 参照。WP STAGINGが無料版のため、Aには移行プラグインの追加が必要 |

---

## 4. ⚠ 本番反映（フェーズ9・9月）

### 4-0. まず前提の確認 ―― 「devの要素だけが消える」のではありません

よくある誤解なので、先に書いておきます。

> ❌ **誤り**：本番に差し替えると、dev固有の要素だけが自動的に消える
> ✅ **正しい**：**ステージングの中身が、本番をまるごと置き換える**

DBを移行する方式の場合、本番にいま入っているもの（投稿・固定ページ・設定・メディア）は
**すべてステージング側の内容で上書き**されます。だから本番のバックアップが必須です。

そして重要なのは、**`/development/` は自動では消えないこと**です。
消えるのではなく「**書き換える作業が必要**」です。放置すると次が全部壊れます。

| 放置すると壊れるもの | 実測値 | 対処 |
|---|---|---|
| **投稿本文の画像URL** | **52箇所**が `…/development/wp-content/uploads/…` でハードコード | 移行時の置換（`/development` を削る） |
| **サイト全体のURL設定** | `home` / `siteurl` に `/development` が入っている | 移行時の置換 |
| **初回体験フォームの限定URL** | `…/development/taiken-2026as9y/` | **公式LINEのあいさつメッセージを差し替える** |
| **noindex** | 有効のまま | 設定 → 表示設定 で解除 |
| **メンテナンスモード** | LightStart 有効のまま | OFF にする |

> ⚠ **限定URLの差し替え忘れが、いちばん実害が大きい**です。
> LINEに載っているURLが `/development/` のままだと、申込者は
> **noindexの付いた旧ステージングを開き続ける**ことになり、申込に気づけません。

### 4-1. 移行の方式を決める（山口様と要相談）

**ステージングにあるのは WP STAGING の無料版**でした（All-in-One WP Migration は未インストール）。
**無料版には「本番へ戻す（Push）」機能がありません**（Pro版の機能）。
そのため、9月の切り替えは次のどちらかを選ぶ必要があります。

| | 方式A：DBまるごと移行 | 方式B：本番でテーマ＋ページを作り直す |
|---|---|---|
| 作業量 | 1〜2時間 | 2〜3時間 |
| 必要なもの | 移行プラグイン（WP STAGING Pro／All-in-One WP Migration 等） | **追加不要** |
| `/development/` の置換 | 52箇所＋設定を一括置換。**漏れると画像が全部消える** | **そもそも発生しない** |
| 本番の既存データ | **全部上書き**（消える危険あり） | **残る**（触った部分だけ変わる） |
| 失敗したときの戻し方 | バックアップから全体復元 | その項目だけ直す |
| 向いている場面 | 短時間で確実に同一にしたい | **安全性を優先したい** |

> 💡 **方式Bが向いている理由**：このテーマは**固定ページの中身をPHPが持っている**設計です。
> つまり本番にテーマを入れてスラッグを合わせるだけで、9ページ分の見た目が再現されます。
> 移すべき実データは「投稿3本の本文・画像18点・キャンペーン投稿・フォームのページ」だけです。
>
> **方式Bを選ぶ場合、この`★次にやること.md`の②〜⑦を本番で再実行する形**になります。

- [ ] 山口様と方式を決める（AかB）

### 4-2. どちらの方式でも必ずやること

- [ ] **本番のバックアップを取得**（これだけは絶対に省略しない）
- [ ] 本番の投稿一覧と突き合わせ（**8月以降に記事が増えていないか**。増えていれば先に取り込む）
- [ ] ステージングの**テスト申込データを削除**（「初回体験の申込」の中身）
- [ ] **noindexを解除**（設定 → 表示設定）
- [ ] **LightStart のメンテナンスモードをOFF**
- [ ] 301リダイレクトを設定（§⑥の4本）
- [ ] **公式LINEのあいさつメッセージの限定URLを、本番のURLに差し替え**
- [ ] 本番で**フォームのテスト送信**をやり直す（メール2通が届くか）
- [ ] Search Console にサイトマップを再送信。**`/blog/` のエラーが出ていないか確認**

### 4-3. 方式Aを選んだ場合の追加確認

- [ ] 置換後に **`/development/` の残骸がゼロ**であることを確認
      （投稿本文52箇所・`home`・`siteurl`・メディアのURL）
- [ ] 画像が1点も欠けていないか、全記事を開いて目視
- [ ] 予約システム（LatePoint）の設定が壊れていないか

---

## 5. ハマりどころ（同じ失敗を繰り返さないために）

実際に発生した問題と原因です。**症状が出たらまずここを疑ってください。**

| 症状 | 原因 | 対処 |
|---|---|---|
| **変更が反映されない** | テーマのバージョンが同じだと、ブラウザとSuper Page Cacheが古いCSSを配り続ける | バージョンに時刻を入れる仕組みに変更済み。**外観→テーマ のバージョンがzipと同じなら更新済み**。あとはキャッシュ削除＋Ctrl+F5 |
| **ページを開いたらコラム一覧が出る** | 固定ページが存在せず404 → 404画面が無かった | 404.php を追加済み。いまは「ページが見つかりません」と出る＝**作り忘れかスラッグ違い** |
| **`/access/` がコラムになる** | 表示設定の「投稿ページ」に誤ったページを指定 | 投稿ページに指定してよいのは**「コラム」だけ** |
| **重要なお知らせがコラム一覧に混ざる** | WordPressは「先頭に固定表示」の投稿を除外設定を無視して差し込む | `ignore_sticky_posts` で対処済み |
| **サブページのレイアウトが崩れる** | 全ページのCSSを1本に結合していて、同名セレクタが上書きし合っていた | ページごとに `css/<名前>.css` を1本読む方式に変更済み |
| **外観→メニューで設定しても反映されない** | このテーマは `wp_nav_menu` を使わず、ヘッダー・フッターとも**固定リンク**で出力している | 管理画面ではなく **dev のHTMLを直して `build_wp_theme.py` を再実行** |
| **「未分類」カテゴリーが削除できない** | WordPressは**既定カテゴリーを削除できない**仕様 | 先に 設定 → 投稿設定 → 既定カテゴリーを「コラム」に変更してから削除 |
| **フォームを送信しても何も届かない** | devのフォームはNetlify用。WordPressでは送信先が存在せず、**画面は正常に見えるのに届かない** | 送信処理を実装済み（`aspath_trial_handle()`）。テーマを更新すれば動く |
| **フォームで「送信が無効になりました」** | ページがキャッシュされ、使い捨ての符号（nonce）が古いまま配られている | Super Page Cache の**除外に追加**→パージ。テーマ側でも自動除外している |

---

## 6. ファイルの場所

| 用途 | 場所 |
|---|---|
| **本ファイル（入口）** | `★次にやること.md` |
| 詳細手順（全フェーズ） | `_wp移行素材/★WP移行_実行手順_統合版.md` |
| ステージングの実測状況 | `_wp移行素材/★ステージング現状診断_2026-08-16.md` |
| ブログ移行の棚卸し | `_wp移行素材/★現行ブログ_完全移行リスト.md` |
| コラム本文の貼り付け用 | `_wp移行素材/★コラム本文_貼り付け用/*.html` |
| **初回体験フォーム（限定URL）** | `_wp移行素材/★初回体験フォーム_限定URL_実行手順.md` |
| キャンペーン投稿の登録 | `_wp移行素材/★お知らせ_営業開始キャンペーン_登録手順.md`（XML同梱） |
| 画像のalt一覧 | `_wp移行素材/★メディア登録_alt一覧_18点.md` |
| 301対応表 | `_wp移行素材/301リダイレクト対応表.md` |
| フォーム設計 | `_wp移行素材/SureForms項目設計表.md`（§8が最新） |
| 仮公開の案内文 | `_wp移行素材/仮公開の手順_ステージング限定公開.md` |
| テーマ生成（PowerShell） | `テーマを作る.ps1` ★推奨 |
| テーマ生成（中身） | `build_wp_theme.py` |
| サイト本体 | `*.html` ＋ `images/`（54点） |

---

## 7. サブPCでClaudeに引き継ぐとき

**会話の履歴そのものは別のPCへ持ち込めません。** 代わりに、このファイルが引き継ぎ書になります。
サブPCでClaudeを開いたら、**クローンしたフォルダを選択**したうえで、下の文をそのまま貼ってください。

```
ASPATHのWordPress移行の続きです。
まず ★次にやること.md を読んで、現状と残作業を把握してください。
そのうえで「2. 残り作業」の①から進めます。

・作業対象：https://aspath-life.com/development/（ステージング）
・本番 aspath-life.com は触らない
・詳細手順は _wp移行素材/ の各mdにあります
```

これだけで、現状・残作業・過去のハマりどころまで引き継がれます。

> 💡 **迷ったらこのファイルを更新してください。** チェックを埋める、気づいたことをメモする、
> それをCommit → Pushしておけば、次にどのPCで開いても最新の状態から再開できます。
> **記憶ではなくファイルに残す**のが、2台運用でいちばん確実です。
