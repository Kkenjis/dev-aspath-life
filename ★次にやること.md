# ★ 次にやること（ASPATH WordPress）

最終更新：2026年8月22日／**このファイルが唯一の入口です。作業を始めるときは必ずここから。**

---

# 🎉 本番公開 完了（2026年8月22日）

**新サイトは `https://aspath-life.com/` で公開中です。**

当初9月予定だった本番切替を前倒しで実施しました。
仮公開（ステージングでの一般公開）は、コメントの消失リスクと同期の手間を避けるため取りやめ、
**本番を直接切り替える方式**を選びました。

## 公開直後の検証結果（すべて合格）

| 項目 | 結果 |
|---|---|
| 全19ページ | ✅ 一般の訪問者として表示OK |
| 画像 | ✅ **249点すべて読み込みOK** |
| 検索エンジン | ✅ 全ページ `index, follow`（フォームのみ noindex） |
| 編集バーの露出 | ✅ なし |
| コメント | ✅ 6件・スタッフバッジ・返信の入れ子とも動作 |
| 旧表記 | ✅ ゼロ（90分／9月30日／9-30／24時間以内） |
| 初回体験フォーム | ✅ 送信・完了画面・記録すべて動作 |
| スマホのロゴ被り | ✅ 重なりなし（当日修正） |

---

## 0. 作業を始める前に（2分）

```
1. GitHub Desktop で Pull origin        ← 必ず最初に
2. 「テーマを作る.ps1」を右クリック →「PowerShell で実行」
3. _wp移行素材/aspath-theme.zip ができる
```

**実行ポリシーで止まる場合**

```powershell
powershell -ExecutionPolicy Bypass -File ".\テーマを作る.ps1"
```

- 本番の管理画面：`https://aspath-life.com/wp-admin/`
- 作業が終わったら **Commit → Push origin**

> **サイトの修正手順**：devのHTMLを直す → `テーマを作る.ps1` → 外観 → テーマ →
> アップロード →「既存のものを置き換える」→ キャッシュ削除。
> **サイトを止める必要はありません。**

---

## 1. いまの構成

| | 場所 | 用途 |
|---|---|---|
| **本番サイト** | `https://aspath-life.com/` | 公開中。ASPATHテーマ |
| **初回体験フォーム** | `https://aspath-life.com/taiken-2026as9y/` | 専用プラグインが担当。noindex |
| ステージング | `https://aspath-life.com/development/` | 確認用。**役目は終了** |

**投稿**：9本（コラム6／お知らせ3、うちキャンペーンは先頭固定）
**固定ページ**：14本（うち旧ページ3本は下記の判断待ち）
**コメント**：6件

---

## 2. 残っている作業

### ⬜ ① 問い合わせフォームの修正（山口様から連絡待ち）

内容が届き次第、対応します。**本番稼働中でも安全に修正できます**
（フォームは他のページから独立しているため、サイトを止める必要はありません）。

**修正の流れ**

1. `trial-entry.html` を修正
2. `テーマを作る.ps1` → `python build_trial_plugin.py`
3. 本番の プラグイン → 新規追加 → アップロード →「プラグインを置き換える」
4. 送信テストで確認

### ⬜ ② テスト申込の削除

```
本番の管理画面 → 初回体験の申込
・公開後テスト 様（2026年8月22日 08:39）
```

### ⬜ ③ 旧ページ3本の扱いを決める

`for-parkinsons-disease` ／ `パーキンソン病の方へ` ／ `for-stroke`

いずれも**現在も表示できる状態**です（新デザインで表示されます）。

| 選択肢 | 結果 |
|---|---|
| **そのまま残す** | 作業ゼロ。検索から来た方も新デザインのページを見られる |
| 転送を設定して削除 | Redirectionプラグインで4本設定。手間10分 |
| 削除だけする | **404になるので非推奨** |

> 当初「301は見送り」としていましたが、**旧ページが生きたまま公開された**ため
> 状況が変わりました。**そのまま残すのが最も安全**です。

### ⬜ ④ Search Console にサイトマップを送信

```
https://aspath-life.com/wp-sitemap.xml
```

数日後、`/blog/` のエラーが出ていないか確認してください
（`blog` は `column` に改名済みのため）。

### ⬜ ⑤ 公式LINEへのURL掲載（山口様に依頼中）

```
https://aspath-life.com/taiken-2026as9y/
```

**このURLは切替前から本番で動いていたため、変更は不要**です。

### ⬜ ⑥ 山口様への確認事項11件

→ `_wp移行素材/★山口様への確認事項_2026-08-18.md`

うち**#1 通知先アドレス／#6 英語対応の範囲**などは未回答です。

---

## 3. 運用のしかた（山口様へお伝えする内容）

| やりたいこと | 手順 |
|---|---|
| **記事を書く** | 投稿 → 新規追加。カテゴリーを「コラム」か「お知らせ」に |
| **重要なお知らせにする** | 投稿の編集画面で「**先頭に固定表示**」にチェック → TOPと一覧の先頭に出て「重要」バッジが付く |
| **コメントを承認する** | コメント → 「承認」をクリック。**山口様の返信は自動で公開**されます |
| **申込を確認する** | メール通知＋管理画面「**初回体験の申込**」。メールが届かなくても記録は残ります |
| **申込者へ返信する** | 通知メールにそのまま返信すれば申込者へ届きます |

---

## 4. ハマりどころ（同じ失敗を繰り返さないために）

実際に発生した問題です。**症状が出たらまずここを疑ってください。**

| 症状 | 原因 | 対処 |
|---|---|---|
| **変更が反映されない** | ブラウザとSuper Page Cacheが古いCSSを配る | テーマのバージョンがzipと同じなら更新済み。キャッシュ削除＋Ctrl+F5 |
| **設定を変えたのに戻る** | Super Page Cache が最長1年キャッシュする | **URL末尾に `?nocache=123` を付けて開く**と切り分けできる |
| **ページを開いたらコラム一覧が出る** | 固定ページが無く404 → 404画面が無かった | `404.php` 追加済み。「見つかりません」＝作り忘れかスラッグ違い |
| **`/access/` がコラムになる** | 表示設定の「投稿ページ」に誤ったページを指定 | 投稿ページは**「コラム」だけ**、ホームページは**「HOME」だけ** |
| **重要なお知らせがコラム一覧に混ざる** | WPは固定表示の投稿を除外設定を無視して差し込む | `ignore_sticky_posts` で対処済み |
| **サブページのレイアウトが崩れる** | 全ページのCSSを1本に結合して上書き事故 | ページごとに `css/<名前>.css` を読む方式に変更済み |
| **メールが届かない** | 送信元が gmail.com のまま他所のサーバーから送信 | Gmail直結（アプリパスワード）で解決済み |
| **フォーム送信が必ず404** | 項目名 `name` がWordPressの予約語と衝突 | 全項目に接頭辞 `at_` を付けて解決済み |
| **送信成功時だけ404** | 保存・メール送信時に他プラグインが $wp_query を壊す | 退避・復元で解決済み |
| **サイトが真っ白（500）** | テーマとプラグインで関数が二重定義 | ガード追加＋ビルド時に全数照合するようにした |
| **未ログインでログイン画面が出る** | WP STAGING の保護（LightStartではない） | WP Staging → 設定 →「管理者認証を無効にする」 |
| **フォームのロゴがヘッダーに隠れる** | ヘッダーが `position:absolute` で重なる設計 | フォームページのみ `relative` に変更済み |

---

## 5. ファイルの場所

| 用途 | 場所 |
|---|---|
| **本ファイル（入口）** | `★次にやること.md` |
| テーマ生成 | `テーマを作る.ps1` ★推奨 ／ `build_wp_theme.py` |
| フォーム用プラグインの生成 | `build_trial_plugin.py` |
| サイト本体 | `*.html` ＋ `images/`（54点） |
| 山口様への確認事項 | `_wp移行素材/★山口様への確認事項_2026-08-18.md` |
| メール設定の記録 | `_wp移行素材/★メールが届くようにする_設定手順.md` |
| コメント機能 | `_wp移行素材/★コメント機能_導入手順.md` |
| フォームの先行設置 | `_wp移行素材/★初回体験フォーム_本番先行設置_手順.md` |
| 本番のバックアップ | `_wp移行素材/現行-本番サイトbak/`（510MB・2026-08-22 03:30） |

---

## 6. 本番を触るときの心得

**バックアップは取得済み**ですが、大きな変更の前には改めて取得してください。

**戻し方**

| 変更内容 | 戻し方 |
|---|---|
| テーマの変更 | 外観 → テーマ → 旧テーマ（Astra）に戻す。**見た目は即座に復帰** |
| プラグインの不具合 | プラグイン → 停止。管理画面が開けない場合はファイルマネージャで
`wp-content/plugins/aspath-trial-form` のフォルダ名を変える |
| 記事・設定 | バックアップから復元 |

**方式Bで切り替えたため、記事・コメント・メディアはすべて本番のものがそのまま使われています。**
データが失われる操作は行っていません。
